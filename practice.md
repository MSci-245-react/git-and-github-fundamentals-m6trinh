# Lab 1 - First Lesson

I learned that JavaScript is important because it runs directly in every modern browser and is what makes websites interactive. It allows the frontend and backend to interact with each other. 

```javascript
0.1+0.2
typeof null
[]==false
```

```
0.30000000000000004
'object'
true
```

## Takeaways
- `0.1 + 0.2` showed me that JavaScript decimals aren't 100% accurate 
- I tried `typeof null` in the REPL and it's still gives the bug result!
- `[] == false` showed me === is usually safer.
